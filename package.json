{
  "name": "QandA",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "13.7.x"
  },
  "scripts": {
    "start": "cd server && node api.js",
    "dev": "cd client && npm start",
    "test": "cd client && npm test",
    "preinstall": "cd server && npm install",
    "install": "cd client && npm install",
    "build": "cd client && npm run build"
  },
  "eslintConfig": {
    "extends": "react-app"
  }
}
